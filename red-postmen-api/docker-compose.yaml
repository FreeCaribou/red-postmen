services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-db-red-postmen
    restart: unless-stopped
    environment:
      POSTGRES_USER: caribou
      POSTGRES_PASSWORD: freeTheCaribou
      POSTGRES_DB: red-postmen
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

